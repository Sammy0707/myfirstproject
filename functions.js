let makeOrder = "bagel";
let topping = "tuna, sweetcorn, lettuce, cheese, onions";

let orderCount = 1;
const takeOrder = (topping, order) => {
 console.log(`${order} with ${topping}`);
 orderCount++;
}
takeOrder(topping, makeOrder)


for( i = 0 ; i < 10 ; i++ ){
    console.log(i);
   }


   let favouriteDrinks = ["Coke", "Fanta", "Tonic"];
console.log(favouriteDrinks[0]);
console.log(favouriteDrinks[1]);
console.log(favouriteDrinks[2]);

let favouriteFilms = [
    "Heat",
    "Troy",
    "Rocky1",
    "Mario",
    "Life in Hell",
   ];
   for  (filmsIndex = 0; filmsIndex < favouriteFilms.length; filmsIndex++) {
        console.log(favouriteFilms[filmsIndex]);
   }
filmIndex.push("J"),
for (filmIndex = 0; filmIndex < favouriteFilms.length;